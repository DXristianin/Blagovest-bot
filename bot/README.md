# Telegram Бот для LatePoint (Blagovest.net)

Telegram бот для отправки уведомлений о бронированиях в музыкальной школе.

## Возможности

- 🔔 Уведомления о новых бронированиях
- 📝 Уведомления об изменениях и отменах
- ⏰ Напоминания перед уроками
- 📅 Просмотр расписания (/today, /week)
- ⚙️ Настройка типов уведомлений
- 🔘 Inline кнопки для быстрых действий
- 🎥 Интеграция с Google Meet

## Установка

### 1. Системные требования

- Python 3.9+
- MySQL/MariaDB (используется WordPress)
- Доступ к серверу с установленным WordPress + LatePoint

### 2. Установка зависимостей

```bash
cd /opt/blagovest-telegram-bot
pip3 install -r requirements.txt
```

### 3. Создание Telegram бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### 4. Конфигурация

Отредактируйте файл `config.py` или установите переменные окружения:

```bash
# Обязательные настройки
export BOT_TOKEN="your_bot_token_here"
export WEBHOOK_SECRET="copy_from_wordpress_plugin"

# Опциональные настройки
export WP_API_URL="https://blagovest.net/wp-json/latepoint-telegram/v1"
export HOST="0.0.0.0"
export PORT="8000"
export REMINDER_MINUTES_BEFORE="60"
```

**ВАЖНО:** Скопируйте `WEBHOOK_SECRET` из настроек WordPress плагина LatePoint Telegram!

### 5. Установка WordPress плагина

1. Плагин уже установлен в `/home/blagovest.net/public_html/wp-content/plugins/latepoint-telegram/`
2. Активируйте плагин в админке WordPress
3. Перейдите в LatePoint → Telegram
4. Введите Bot Token
5. Введите Bot Service URL (например: `http://localhost:8000`)
6. Скопируйте Webhook Secret в настройки бота

### 6. Запуск бота

#### Вручную (для тестирования):

```bash
cd /opt/blagovest-telegram-bot
python3 bot.py
```

#### Через systemd (автозапуск):

```bash
# Перезагрузить конфигурацию systemd
systemctl daemon-reload

# Включить автозапуск
systemctl enable telegram-bot-blagovest

# Запустить бота
systemctl start telegram-bot-blagovest

# Проверить статус
systemctl status telegram-bot-blagovest

# Просмотр логов
journalctl -u telegram-bot-blagovest -f
```

## Использование

### Для учителей и учеников

1. Перейдите в личный кабинет на blagovest.net
2. Перейдите в раздел LatePoint → Telegram
3. Нажмите "Сгенерировать токен регистрации"
4. Скопируйте команду вида `/start YOUR_TOKEN`
5. Найдите бота в Telegram и отправьте эту команду
6. Готово! Теперь вы будете получать уведомления

### Доступные команды

- `/start <token>` - Регистрация в боте
- `/today` - Уроки на сегодня
- `/week` - Уроки на неделю
- `/settings` - Настройки уведомлений
- `/help` - Справка

## Структура проекта

```
/opt/blagovest-telegram-bot/
├── bot.py                    # Главный файл
├── config.py                 # Конфигурация
├── requirements.txt          # Зависимости
├── database/
│   ├── models.py            # Модели БД
│   └── db.py                # Менеджер БД
├── services/
│   ├── wordpress_api.py     # WordPress API клиент
│   └── scheduler.py         # Планировщик напоминаний
├── handlers/
│   ├── commands.py          # Обработчики команд
│   ├── callbacks.py         # Обработчики callback
│   └── notifications.py     # Обработчик уведомлений
└── logs/
    └── bot.log              # Логи бота
```

## База данных

Бот использует SQLite для хранения:
- Зарегистрированных пользователей
- Настроек уведомлений
- Логов отправленных сообщений
- Отправленных напоминаний (чтобы не дублировать)

База создается автоматически при первом запуске в файле `bot_data.db`.

## Настройка уведомлений

Каждый пользователь может настроить типы уведомлений через команду `/settings`:

- ✅/❌ Новые бронирования
- ✅/❌ Изменения
- ✅/❌ Отмены
- ✅/❌ Напоминания
- ⏰ Время напоминаний (15/30/60/120/180 минут до начала)

## Troubleshooting

### Бот не запускается

1. Проверьте, что установлены все зависимости:
   ```bash
   pip3 install -r requirements.txt
   ```

2. Проверьте конфигурацию в `config.py`

3. Убедитесь, что токен бота правильный

### Уведомления не приходят

1. Проверьте, что WordPress плагин активирован
2. Проверьте, что Bot Service URL правильный
3. Проверьте, что Webhook Secret совпадает
4. Проверьте логи бота:
   ```bash
   tail -f /opt/blagovest-telegram-bot/logs/bot.log
   ```

### Пользователь не может зарегистрироваться

1. Проверьте, что токен не истек (15 минут)
2. Сгенерируйте новый токен в личном кабинете
3. Проверьте связь с WordPress API

## Мониторинг

### Просмотр статуса

```bash
systemctl status telegram-bot-blagovest
```

### Просмотр логов

```bash
# Последние 100 строк
journalctl -u telegram-bot-blagovest -n 100

# В реальном времени
journalctl -u telegram-bot-blagovest -f

# Логи в файле
tail -f /opt/blagovest-telegram-bot/logs/bot.log
```

### Health check

```bash
curl http://localhost:8000/health
```

## Безопасность

- Webhook защищен секретным ключом
- Токены регистрации действительны 15 минут
- Логирование всех действий
- Проверка прав доступа при изменении бронирований

## Обновление

```bash
# Остановить бота
systemctl stop telegram-bot-blagovest

# Обновить код
cd /opt/blagovest-telegram-bot
# (обновите файлы)

# Обновить зависимости (если нужно)
pip3 install -r requirements.txt --upgrade

# Запустить бота
systemctl start telegram-bot-blagovest
```

## Поддержка

По вопросам обращайтесь к администратору сервера.
