# Руководство по настройке и запуску Telegram бота для LatePoint

## ✅ Что уже сделано

### 1. WordPress плагин
- ✅ Создан плагин в `/home/blagovest.net/public_html/wp-content/plugins/latepoint-telegram/`
- ✅ Реализованы hooks для отслеживания событий LatePoint
- ✅ Созданы REST API endpoints для взаимодействия с ботом
- ✅ Добавлен UI для генерации токенов регистрации

### 2. Python бот
- ✅ Создана структура проекта в `/opt/blagovest-telegram-bot/`
- ✅ Установлены все зависимости Python
- ✅ Реализованы все команды бота (/start, /today, /week, /settings)
- ✅ Реализованы уведомления о бронированиях
- ✅ Реализована система напоминаний
- ✅ Создан systemd service для автозапуска

## 📋 Что нужно сделать для запуска

### Шаг 1: Создать Telegram бота

1. Откройте Telegram и найдите **@BotFather**

2. Отправьте команду:
   ```
   /newbot
   ```

3. Следуйте инструкциям:
   - Введите имя бота (например: `Blagovest Music School Bot`)
   - Введите username бота (например: `blagovest_music_bot`)

4. BotFather даст вам **токен бота** вида:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

5. **Сохраните этот токен!** Он понадобится на следующем шаге.

### Шаг 2: Настроить конфигурацию бота

Отредактируйте файл `/opt/blagovest-telegram-bot/config.py`:

```bash
nano /opt/blagovest-telegram-bot/config.py
```

Найдите и замените следующие строки:

```python
# Вместо 'YOUR_BOT_TOKEN_HERE' вставьте токен от BotFather
BOT_TOKEN = os.getenv('BOT_TOKEN', '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz')

# URL WordPress API (обычно не требует изменений)
WP_API_URL = os.getenv('WP_API_URL', 'https://blagovest.net/wp-json/latepoint-telegram/v1')

# Webhook Secret - скопируйте из WordPress после активации плагина
WEBHOOK_SECRET = os.getenv('WEBHOOK_SECRET', 'TEMPORARY_SECRET')
```

**ВАЖНО:** На данном этапе можно оставить WEBHOOK_SECRET как 'TEMPORARY_SECRET'. Вы скопируете правильное значение после активации WordPress плагина.

### Шаг 3: Активировать WordPress плагин

1. Перейдите в админку WordPress: `https://blagovest.net/wp-admin/`

2. Перейдите в **Плагины** → **Установленные плагины**

3. Найдите плагин **LatePoint Telegram Notifications**

4. Нажмите **Активировать**

### Шаг 4: Настроить WordPress плагин

1. После активации перейдите в **LatePoint** → **Telegram**

2. Заполните настройки:
   - **Bot Token**: Вставьте токен от BotFather
   - **Bot Service URL**: `http://localhost:8000`
   - **Webhook Secret**: Скопируйте отображаемый секретный ключ

3. Нажмите **Сохранить настройки**

### Шаг 5: Обновить Webhook Secret в боте

Теперь вернитесь к конфигурации бота и обновите WEBHOOK_SECRET:

```bash
nano /opt/blagovest-telegram-bot/config.py
```

Замените:
```python
WEBHOOK_SECRET = os.getenv('WEBHOOK_SECRET', 'СКОПИРОВАННЫЙ_SECRET_ИЗ_WORDPRESS')
```

### Шаг 6: Запустить бота

#### Вариант A: Запуск вручную (для тестирования)

```bash
cd /opt/blagovest-telegram-bot
python3 bot.py
```

Вы должны увидеть:
```
INFO - Starting Telegram bot...
INFO - Database initialized
INFO - WordPress API session initialized
INFO - Reminder scheduler started
INFO - Telegram bot started successfully!
INFO - Web server started on 0.0.0.0:8000
INFO - Bot is running in webhook mode...
INFO - Starting polling...
```

Нажмите `Ctrl+C` для остановки.

#### Вариант B: Запуск через systemd (рекомендуется)

```bash
# Перезагрузить конфигурацию systemd
systemctl daemon-reload

# Включить автозапуск
systemctl enable telegram-bot-blagovest

# Запустить бота
systemctl start telegram-bot-blagovest

# Проверить статус
systemctl status telegram-bot-blagovest
```

Вы должны увидеть:
```
● telegram-bot-blagovest.service - Blagovest Telegram Bot for LatePoint
     Loaded: loaded
     Active: active (running)
```

### Шаг 7: Проверить работу

#### Проверка health endpoint:
```bash
curl http://localhost:8000/health
```

Должен вернуться JSON:
```json
{"status":"ok","service":"telegram-bot","version":"1.0.0"}
```

#### Проверка логов:
```bash
tail -f /opt/blagovest-telegram-bot/logs/bot.log
```

#### Тестирование регистрации:

1. Перейдите в личный кабинет WordPress: `https://blagovest.net/wp-admin/`
2. Перейдите в **LatePoint** → **Telegram**
3. Нажмите **Сгенерировать токен регистрации**
4. Скопируйте команду вида `/start ABC123...`
5. Найдите вашего бота в Telegram
6. Отправьте скопированную команду
7. Бот должен ответить сообщением об успешной регистрации

## 🔧 Управление ботом

### Просмотр статуса
```bash
systemctl status telegram-bot-blagovest
```

### Остановка бота
```bash
systemctl stop telegram-bot-blagovest
```

### Перезапуск бота
```bash
systemctl restart telegram-bot-blagovest
```

### Просмотр логов в реальном времени
```bash
journalctl -u telegram-bot-blagovest -f
```

### Просмотр логов бота
```bash
tail -f /opt/blagovest-telegram-bot/logs/bot.log
```

## 📱 Регистрация учителей и учеников

### Для администратора:

1. Каждый учитель и ученик должен иметь аккаунт WordPress
2. Учителя должны быть связаны с агентами в LatePoint
3. Ученики должны быть связаны с клиентами в LatePoint

### Для пользователей:

1. Войти в личный кабинет на blagovest.net
2. Перейти в **LatePoint** → **Telegram**
3. Сгенерировать токен регистрации
4. Найти бота в Telegram
5. Отправить команду `/start <токен>`
6. Готово!

## 🎯 Возможности бота

### Для всех пользователей:
- `/start <token>` - Регистрация в боте
- `/today` - Уроки на сегодня
- `/week` - Уроки на неделю
- `/settings` - Настройки уведомлений
- `/help` - Справка

### Автоматические уведомления:
- 🔔 Новые бронирования (с Google Meet ссылкой)
- 📝 Изменения времени или даты
- ❌ Отмены
- ⏰ Напоминания за 60 минут до начала (настраивается)

### Inline кнопки:
- 📋 Детали бронирования
- ✅ Подтвердить (только для учителей)
- ❌ Отменить

## ❗ Troubleshooting

### Бот не запускается

**Проблема**: `Bot token not configured!`
**Решение**: Проверьте, что вы вставили токен от BotFather в `config.py`

**Проблема**: `Webhook secret not configured!`
**Решение**: Скопируйте webhook secret из настроек WordPress плагина

**Проблема**: `ModuleNotFoundError`
**Решение**: Переустановите зависимости:
```bash
cd /opt/blagovest-telegram-bot
pip3 install -r requirements.txt
```

### Уведомления не приходят

1. Проверьте, что WordPress плагин активирован
2. Проверьте, что Bot Service URL правильный: `http://localhost:8000`
3. Проверьте, что Webhook Secret совпадает
4. Проверьте логи WordPress: `/home/blagovest.net/public_html/wp-content/debug.log`
5. Проверьте логи бота: `/opt/blagovest-telegram-bot/logs/bot.log`

### Пользователь не может зарегистрироваться

**Проблема**: `Invalid token`
**Решение**: Токен мог истечь (15 минут). Сгенерируйте новый токен.

**Проблема**: `User not found`
**Решение**: Убедитесь, что пользователь имеет аккаунт WordPress и связан с LatePoint (агент или клиент).

### Проверка подключения к WordPress API

```bash
curl "https://blagovest.net/wp-json/latepoint-telegram/v1/user-info?chat_id=123456789"
```

Должен вернуться JSON с информацией о пользователе или ошибкой.

## 📊 Мониторинг

### Статистика из базы данных

```bash
cd /opt/blagovest-telegram-bot
python3 -c "
from database.db import db
import asyncio

async def stats():
    users = await db.get_all_users()
    print(f'Зарегистрированных пользователей: {len(users)}')
    agents = await db.get_users_by_type('agent')
    print(f'Учителей: {len(agents)}')
    customers = await db.get_users_by_type('customer')
    print(f'Учеников: {len(customers)}')

asyncio.run(stats())
"
```

## 🎉 Готово!

Теперь ваш Telegram бот полностью настроен и готов к работе!

Учителя и ученики могут регистрироваться и получать уведомления о всех бронированиях.

---

**По всем вопросам обращайтесь к администратору сервера.**
